apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  labels:
    strimzi.io/cluster: '{{ kafka_cluster_name }}'
  name: events
  namespace: '{{ cluster_project }}'
spec:
  config: {}
  partitions: 10
  replicas: 3
  topicName: events
